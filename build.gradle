plugins {
    id 'com.android.application'
}

android {
    namespace 'com.quizpilot.fast'
    compileSdk 35

    defaultConfig {
        applicationId 'com.quizpilot.fast'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '0.1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
